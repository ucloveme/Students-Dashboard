<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Student's Dashboard</title>
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

</head>
<body>
  <div class="container">
    <nav class="sidebar">
        <div class="profile">
          <img src="/uploads/<%= student.profilePic%>" alt="Profile" width="150" />
            <h3><%= student.fullName %></h3>
            <p>ID: <%=student.id%></p>
        </div>
        
        <ul class="nav-links">
            <li class="active"><a href="/dashboard"><i class="fas fa-home"></i> Dashboard</a></li>
            <li><a href="/edit-profile"><i class="fa fa-pencil-square" aria-hidden="true"></i>Edit Profile</a></li>
            <li><a href="/logout"><i class="fa fa-sign-out" aria-hidden="true"></i>Logout</a></li>
        </ul>

        <form class="delete-account" action="/delete-account" method="POST" onsubmit="return confirm('Are you sure you want to delete your account? This cannot be undone.');">
          <button type="submit" class="btn btn-danger">Delete Account</button>
        </form>

    </nav>

    <main class="content">
      <header>
          <div class="search-bar">
              <input type="text" placeholder="Search...">
              <button><i class="fas fa-search"></i></button>
          </div>
          <div class="notifications">
              <i class="fas fa-bell"></i>
              <span class="badge">3</span>
          </div>
      </header>

      <div class="dashboard-content">
        <h1>Welcome, <%=student.fullName %>!</h1>
      </div>

      <div class="quick-stats">
        <div class="stat-card">
            <h3>Current GPA</h3>
            <p class="stat-value">3.8</p>
        </div>
        <div class="stat-card">
            <h3>Courses</h3>
            <p class="stat-value">5</p>
        </div>
        <div class="stat-card">
            <h3>Pending Assignments</h3>
            <p class="stat-value">7</p>
        </div>
        <div class="stat-card">
            <h3>Attendance</h3>
            <p class="stat-value">92%</p>
        </div>
    </div>
    
    <div class="dashboard-grid">
        <div class="dashboard-card upcoming-assignments">
            <h2>Upcoming Assignments</h2>
            <ul id="assignments-list">
                <!-- Will be populated by JavaScript -->
            </ul>
        </div>
        
        <div class="dashboard-card recent-grades">
            <h2>Recent Grades</h2>
            <ul id="grades-list">
                <!-- Will be populated by JavaScript -->
            </ul>
        </div>
        
        <div class="dashboard-card calendar">
            <h2>Calendar</h2>
            <div id="mini-calendar">
                <!-- Will be populated by JavaScript -->
            </div>
        </div>
        
        <div class="dashboard-card announcements">
            <h2>Announcements</h2>
            <ul id="announcements-list">
                <!-- Will be populated by JavaScript -->
            </ul>
        </div>
    </div>
    </main>
  </div>

</body>
</html>
